[how to build]

1. export PATH="/opt/hbpk/bin:$PATH"
2. cd base && make install
3. cd build[/PACKAGE] && make install

[command usage]

install:
% hbpk-add PACKAGE-VER.pmgr

uninstall:
% hbpk-del PACKAGE

list installed packages:
% hbpk-ls [package-name]

Thank you!
